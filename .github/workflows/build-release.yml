name: 构建和发布

on:
  push:
    tags:
      - 'v*'  # 当推送 v 开头的 tag 时触发（如 v1.0.0）

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
    
    - name: 设置 Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip wheel
        pip install -r requirements.txt
        pip install pyinstaller
    
    - name: 自动更新版本号（从 tag 提取）
      run: |
        # 从 tag 提取版本号（去掉开头的 v）
        $tagName = "${{ github.ref_name }}"
        $version = $tagName -replace '^v', ''
        
        Write-Host "📌 Tag: $tagName"
        Write-Host "📌 提取的版本号: $version"
        
        # 读取原有的 __version__.py
        $versionFile = Get-Content "__version__.py" -Raw
        
        # 替换版本号
        $versionFile = $versionFile -replace '__version__ = ".*"', "__version__ = `"$version`""
        
        # 写回文件
        $versionFile | Set-Content "__version__.py" -NoNewline
        
        Write-Host "✅ 版本号已自动更新为: $version"
        
        # 显示更新后的内容
        Write-Host "`n更新后的 __version__.py:"
        Get-Content "__version__.py"
      shell: pwsh
    
    - name: 动态生成 PyInstaller spec 文件
      run: |
        $specContent = @"
        # -*- mode: python ; coding: utf-8 -*-

        block_cipher = None

        datas = [
            ('label.ico', '.'),
            ('label.png', '.'),
        ]

        hiddenimports = ['PIL._tkinter_finder']

        excludes = [
            'tkinter', 'matplotlib', 'numpy', 'pandas', 'scipy',
            'IPython', 'notebook', 'pytest', 'unittest', 'setuptools', 'distutils',
            'PyQt6.QtWebEngine', 'PyQt6.QtWebEngineCore', 'PyQt6.QtWebEngineWidgets',
            'PyQt6.QtMultimedia', 'PyQt6.QtMultimediaWidgets',
            'PyQt6.Qt3D', 'PyQt6.QtQuick', 'PyQt6.QtQml',
            'PyQt6.QtBluetooth', 'PyQt6.QtNfc', 'PyQt6.QtPositioning', 'PyQt6.QtSensors',
            'xml.etree', 'pydoc', 'doctest', '_pytest', 'test', 'tests',
        ]

        a = Analysis(
            ['label_gui_qt.py'],
            pathex=[],
            binaries=[],
            datas=datas,
            hiddenimports=hiddenimports,
            hookspath=[],
            hooksconfig={},
            runtime_hooks=[],
            excludes=excludes,
            win_no_prefer_redirects=False,
            win_private_assemblies=False,
            cipher=block_cipher,
            noarchive=False,
            optimize=2,
        )

        pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

        exe = EXE(
            pyz,
            a.scripts,
            [],
            exclude_binaries=True,
            name='Label-Auto-Reformat-Printing',
            debug=False,
            bootloader_ignore_signals=False,
            strip=False,
            upx=True,
            console=False,
            disable_windowed_traceback=False,
            argv_emulation=False,
            target_arch=None,
            codesign_identity=None,
            entitlements_file=None,
            icon=['label.ico'],
        )

        coll = COLLECT(
            exe,
            a.binaries,
            a.datas,
            strip=False,
            upx=True,
            upx_exclude=[],
            name='Label-Auto-Reformat-Printing',
        )
        "@
        
        $specContent | Out-File -FilePath "label_bundle.spec" -Encoding UTF8
        Write-Host "✅ spec 文件已生成"
      shell: pwsh
    
    - name: 使用 PyInstaller 打包
      run: |
        pyinstaller label_bundle.spec
    
    - name: 压缩打包文件
      run: |
        cd dist
        Compress-Archive -Path "Label-Auto-Reformat-Printing" -DestinationPath "Label-Auto-Reformat-Printing-Windows-x64.zip"
    
    - name: 获取版本号
      id: get_version
      run: |
        $version = "${{ github.ref_name }}"
        echo "version=$version" >> $env:GITHUB_OUTPUT
      shell: pwsh
    
    - name: 创建 Release
      uses: softprops/action-gh-release@v1
      with:
        files: dist/Label-Auto-Reformat-Printing-Windows-x64.zip
        body: |
          ## 🏷️ 标签打印工具 | Label Auto Reformat Printing ${{ steps.get_version.outputs.version }}
          
          ### 📦 下载说明 | Download Instructions
          - **Windows 用户**：下载 `Label-Auto-Reformat-Printing-Windows-x64.zip` 并解压
          - **Windows Users**: Download `Label-Auto-Reformat-Printing-Windows-x64.zip` and extract
          - 解压后运行 `Label-Auto-Reformat-Printing.exe` 即可使用
          - Run `Label-Auto-Reformat-Printing.exe` after extraction
          
          ### ✨ 主要功能
          - Material Design 风格界面
          - 支持中文/泰语双语
          - PDF 预览和生成
          - 参数自动保存
          - 精致的加载动画
          
          ### 📋 更新内容
          
          #### 🎨 UI 视觉优化
          - 新增 GroupBox 边框，提升界面层次感
          - 预览区域使用渐变背景，边框加粗
          - 添加专业的加载动画（旋转指示器 + 半透明遮罩）
          
          #### 🔧 代码质量改进
          - 添加配置参数验证
          - 添加输入参数验证
          - UI 常量集中管理
          - 优化临时文件清理
          - 提取重复代码
          - 窗口缩放性能优化
          
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

